{
  "name": "contour-integration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Sandbox and worked-example gallery for contour integration and the residue theorem, including the evaluation of real definite integrals in closed form. Fifth app in the suite; rides the shared @cas/* packages. See docs/contour-integration/PLAN.md.",
  "license": "MIT",
  "author": "Andrew Graven <andrew@graven.com>",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cas/core": "workspace:*",
    "@cas/exact": "workspace:*",
    "@cas/expr": "workspace:*",
    "@cas/gpu": "workspace:*",
    "@cas/rigor": "workspace:*",
    "@cas/ui": "workspace:*"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
