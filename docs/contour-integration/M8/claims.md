# M8 — every sentence the app composes

**Generated. Do not edit this file by hand** — it is a view of `apps/contour-integration/test/helpers/claimsProposals.ts`,
which is what step 0.5b reads. To change a proposal, change that table and regenerate:

```
cd apps/contour-integration && M8_WRITE_CLAIMS=1 pnpm exec vitest run test/claimsDoc.test.ts
```

## What this is, and what is being asked

Step 0.5 of [the M8 plan](../M8-plan.md) rewrites the app's prose into terse, textbook-neutral
language. These sentences are not decoration: they are the Closing Ledger's **assertions** about
whether a contour argument is complete, so the owner approves the wording before it changes.

**What to read.** The *today* column is the live code. The *proposed* column is a draft, taken from
[`review-inputs/content-review.md`](review-inputs/content-review.md) §2 where that review wrote one.
A **—** means no replacement is proposed yet: either the sentence is fine as it stands, or it needs a
decision. The *flags* column is computed against the plan's four rules — no capitalised emphasis, no
citations of research notes or ledger passes, no lemma numbers `L1`…`L6`, no house jargon — plus a
fifth note, `maths undelimited`, marking a sentence whose mathematics has to move inside `$…$` when
the shell starts typesetting. **A row with a proposal is flagged on the PROPOSAL**, since that is the
sentence that will ship.

**How to reply.** Anything: a line-by-line edit, a sweep of "these are fine, change only the flagged
ones", or a rule you want applied that is not in the list.

**Status:** 60 of 72 proposed in the ledger's own
sentences; 98 sentences across the whole document still break at least one rule.

---

## The five decisions, if you would rather not read 200 rows

Most of what the tables below flag is one habit repeated, so each rule is offered as a blanket
decision with its count and a sample. Approving a rule here settles every row it names; the tables
are then only for the sentences you want to word differently.

- **De-shout every emphasised word.** A single word in capitals for emphasis — `the orders ADD`, `the bound's VALUE` — becomes ordinary type. Names keep their capitals. **29 sentences, 4 already drafted below.**
  - `method 13` — imported, not derived here — on Im z = b/2 the integrand collapses to e^{−x²−b²/4}; the remaining ∫ℝe^{−x²}dx = Γ(1/2) = √π is IMPORTED, not derived here
  - `method 14` — imported, not derived here — on this ray e^{izⁿ} = e^{−tⁿ}; ∫₀^∞e^{−tⁿ}dt = Γ(1+1/n) is IMPORTED (the real substitution u = tⁿ), and the leading minus is the reversed traversal

- **Delete every internal citation.** `research 06 §2.1`, `Pass 5`, `finding D-2`: a reader has no such documents. Each is replaced by the statement it was citing, or removed. **4 sentences, 4 already drafted below.**
  - `method 4` — declared by its role; the coefficient enters Pass 5's M rather than the right-hand side
  - `method 10` — exact winding numbers against exact exponents, summed over ℚ — the admissibility arithmetic of research 06 §2.1(b), read along the contour

- **Name the lemmas rather than numbering them.** `L4` becomes *the indentation lemma*, `L5` *the large-arc lemma*, `L3` *Jordan’s lemma*. **10 sentences, 5 already drafted below.**
  - `method 16` — L6 (oscillatory form), with the range decided in exact ℚ
  - `method 17` — L4 at a simple pole; the sign comes from the signed swept angle (θ₁ − θ₀), which is the i·ε prescription in geometric form

- **Drop the house words.** `the solve`, `the family`, `the record`, `golden value` name parts of this program, not of the mathematics. **4 sentences, 3 already drafted below.**
  - `provenance 42` — refusing would be wrong (the answer is correct) and warning would be wrong (nothing is uncertain); what the escalation costs the record is a DECLARED merged order and residue, both checked against the engine's own

- **Every formula moves inside `$…$`** and is typeset. Text outside the dollars stays text. **103 sentences, 13 already drafted below.**
  - `bound claim 1` — \|∫ over the arc\| ≤ 5.635e-2 at R = 4, and → 0 as R → ∞, because deg Q − deg P = 4 ≥ 2 makes the bound O(R^-3)
  - `bound claim 2` — \|∫ over the arc\| ≤ 6.629e+0 at ρ = 4.000e+0, and → 0 as R → ∞, because the bound is O(ρ^(-3)·(ln ρ)^2) and a logarithm is weaker than every power

**A sixth, which no rule catches.** Several sentences argue with the reader rather than stating a
result — *"one number implying the other is the conflation the dogbone exists to break"*, *"the
single commonest error in the subject"*. The proposed replacements drop them. Say if you would
rather keep any.

---

## 1. Ledger row claims

The one sentence each row of the ledger asserts. `{name}` is a substituted value — a piece's name, a
count, a measured number — so the wording is everything around it.

| where | today | proposed | flags |
|---|---|---|---|
| `legality.avoid-singularities` | the contour must avoid every singularity of the integrand | **—** |  |
| `legality.closed` | the contour is closed and its orientation is declared | the contour is closed (orientation as drawn) |  |
| `legality.not-closed` | the contour does not close | the contour is not closed |  |
| `legality.clearance` | every singularity is clear of the contour (nearest at {nearest}) | no singularity lies on the contour (nearest distance {nearest}) |  |
| `legality.entire` | the integrand is entire, so there is no singularity for the contour to be clear of | the integrand is entire; there are no singularities |  |
| `legality.kernel-band` | the kernel has a pole at every integer, and this contour reaches too many of them to check | $\pi\cot\pi z$ has a pole at every integer, and the contour reaches too many of them to enumerate |  |
| `legality.cuts-admissible` | the cut system is admissible — {detail} | the branch cuts make the integrand single-valued off them — {detail} |  |
| `legality.cuts-inadmissible` | the cut system is not admissible: {detail} | the branch cuts do not make the integrand single-valued: {detail} |  |
| `legality.monodromy-undecided` | the winding about a branch point is undecided, so the monodromy along the contour is too | a winding number about a branch point could not be decided, so neither could the monodromy |  |
| `legality.monodromy-off-sheet` | the contour winds about a branch point and does not close on one sheet ({turns}) | the integrand is not single-valued along the contour ({turns}) |  |
| `legality.monodromy-on-sheet` | the contour winds about a branch point and still closes on one sheet (Σ n(γ,bⱼ)·αⱼ = {sum} ∈ ℤ) | the integrand is single-valued along the contour: $\sum_j \operatorname{Ind}_\gamma(b_j)\,\alpha_j = {sum} \in \mathbb{Z}$ |  |
| `legality.cuts-clear` | no piece of the contour meets a branch cut, except where it ends on one | no piece crosses a branch cut |  |
| `legality.cuts-sided` | every piece that meets a branch cut declares the side it runs on ({declared}) | each piece meeting a cut is assigned a side ({declared}) |  |
| `legality.cut-grazed` | {piece} grazes the cut '{cut}', so it has no side to declare | {piece} touches the cut $\Gamma$ tangentially, so it has no side |  |
| `legality.cut-crossed` | {piece} {how} the cut '{cut}' without declaring which side it runs on | {piece} crosses the cut $\Gamma$ with no side assigned |  |
| `legality.cut-invariance-one` | ∮ is unchanged by moving this cut, while they stay clear of the contour | $\oint_\gamma f(z)\,dz$ does not depend on where the cut runs, while the cut avoids $\gamma$ |  |
| `legality.cut-invariance-many` | ∮ is unchanged by moving these cuts, while they stay clear of the contour | $\oint_\gamma f(z)\,dz$ does not depend on where the cuts run, while they avoid $\gamma$ |  |
| `catch.enclosed-one` | 1 singularity is enclosed, with an exactly decided winding number | $\operatorname{Ind}_\gamma(a) \neq 0$ at 1 singularity, decided exactly |  |
| `catch.enclosed-many` | {n} singularities are enclosed, each with an exactly decided winding number | $\operatorname{Ind}_\gamma(a) \neq 0$ at {n} singularities, each decided exactly |  |
| `catch.winding-undecided` | a winding number could not be decided | $\operatorname{Ind}_\gamma(a)$ could not be decided — a pole lies too close to $\gamma$ |  |
| `catch.residues-exact` | every enclosed residue is known exactly | every enclosed residue is exact |  |
| `catch.residues-exact-kernel` | every enclosed residue is known exactly — the kernel's at each integer, the cofactor's as an exact quotient | every enclosed residue is exact — the kernel's at each integer, and $K(z_j)\operatorname{Res}(f, z_j)$ at each pole of $f$ |  |
| `catch.residues-exact-merged` | every enclosed residue is known exactly — the kernel's at each integer, and the MERGED one from the Laurent route | every enclosed residue is exact, including the merged pole, whose residue comes from the Laurent expansion of the product |  |
| `catch.sum-exact` | Σ Res is known exactly, though no individual residue is expressible | the residue sum is exact — the individual residues lie outside $\mathbb{Q}(i)(\sqrt{d})$, the sum does not |  |
| `catch.residues-inexact` | not every residue is known exactly, so the total is an estimate | some residues are numerical, so the total is an estimate |  |
| `catch.escalation` | a stated hypothesis FAILS and a stronger argument applies: {to}, over {collisions} | $f$ has a pole where the kernel has one; the two combine into a single pole, whose residue comes from the Laurent expansion of the product ({collisions}) |  |
| `kill.target` | {piece} is the target — it is what the argument solves for | {piece}: the target |  |
| `kill.l4-inapplicable` | {piece} is an indentation, but L4 does not apply here | {piece}: the indentation lemma does not apply |  |
| `kill.l5-unreadable` | {piece} is declared L5, but f could not be read as (Σ Nₖ e^{iaₖz})/D | {piece}: $f$ was not recognised in the form $\left(\sum_k N_k e^{i a_k z}\right)/D$ |  |
| `kill.l5-no-limit` | {piece} is declared L5, but z·f(z) has no limit along it | {piece}: $z f(z)$ has no limit on the arc |  |
| `kill.reproduces` | {piece} reproduces the target, as a multiple the solve reads off the family | {piece}: a constant multiple of the target |  |
| `kill.imported` | {piece} is {value} — imported, not derived here | {piece} $= ${value}$ — a known integral, not derived here |  |
| `kill.computed` | {piece} is computed directly ({length} long) | {piece}: evaluated numerically (length {length}) |  |
| `kill.sweep-unreadable` | {piece} must vanish, but its sweep is not an exact multiple of π and no bound can be stated | {piece}: no bound is available — the arc's angle is not a rational multiple of $\pi$ with denominator at most 12 |  |
| `kill.no-lemma` | {piece} must vanish, but no lemma here applies to this integrand | {piece}: no bound is available for this integrand |  |
| `cover.on-contour` | the target appears as a labelled piece of the closed contour | the target is a piece of the contour |  |
| `cover.in-sum` | {id} is a TERM of the residue sum — the kernel's poles at the integers — not a piece of the contour | the target is the sum of the residues at the integers, not a piece of the contour |  |
| `cover.in-sum-weighted` | {id} is a TERM of the residue sum — the kernel's poles at the integers — not a piece of the contour, at weight {weight} | the target is the sum of the residues at the integers, not a piece of the contour (weight {weight}) |  |
| `cover.none` | no piece is marked as the target, so the ledger reports the closed-contour value itself | no target is designated; the closed-contour integral is reported |  |

## 2. Repairs

Shown beneath a failing row: what to do about it. Never seen in the gallery, because every record
closes; these are the sandbox's.

| where | today | proposed | flags |
|---|---|---|---|
| `repair 1` | indent the contour around the singularity, or move it | Indent around the singularity, or move the contour. |  |
| `repair 2` | join the last piece back to the first | **—** |  |
| `repair 3` | shrink the contour, or reduce the limit parameter | Reduce $N$. |  |
| `repair 4` | indent the contour around the branch point (a keyhole), or take in the whole bounded component so the exponents sum to an integer (a dogbone) | Exclude the branch point (a keyhole), or enclose the whole cut (a dogbone). |  |
| `repair 5` | move the cut clear of the contour, or move the contour | Move the cut clear of the contour, or move the contour. |  |
| `repair 6` | tag this segment `above` or `below`, or move the cut | Assign the piece to the upper or lower side of the cut, or move the cut. |  |
| `repair 7` | move the contour clear of the pole | **—** |  |
| `repair 8` | L4 needs a simple pole at the centre of the arc; at order ≥ 2 no limit exists and no principal value does either | The indentation lemma requires a simple pole; at order $\ge 2$ the limit does not exist, and neither does the principal value. |  |
| `repair 9` | L5 needs z·f(z) → L uniformly; without it the arc's contribution is not a number the argument can use | The large-arc lemma requires $z f(z) \to L$ uniformly on the arc. |  |
| `repair 10` | the numeric value still stands, but the limit is not established | **—** |  |
| `repair 11` | close the contour through the other half-plane | **—** |  |
| `repair 12` | this lemma is too weak here — a sharper one may still apply | The ML-estimate does not vanish; Jordan's lemma or an indentation may still apply. |  |

## 3. Headlines

The single sentence above the ledger.

| where | today | proposed | flags |
|---|---|---|---|
| `headline · closes` | This argument closes. | The argument is complete. |  |
| `headline · sandbox` | The closed-contour value is established exactly. | $\oint_\gamma f(z)\,dz$ is established exactly. |  |
| `headline · incomplete` | This argument is incomplete. | The argument is incomplete. |  |
| `headline · fails LEGALITY` | This argument does not close: the hypotheses do not hold. | The argument is incomplete: the residue theorem does not apply. |  |
| `headline · fails CATCH` | This argument does not close: a winding number could not be decided. | The argument is incomplete: a residue is not determined. |  |
| `headline · fails KILL` | This argument does not close: a boundary term is not disposed of. | The argument is incomplete: a boundary term does not vanish. |  |
| `headline · fails COVER` | This argument does not close: the target is not a piece of the contour. | The argument is incomplete: the target is not on the contour. |  |

## 4. Derivation stages

The seven headings of the generated derivation, and the paragraph under each.

| where | today | proposed | flags |
|---|---|---|---|
| `stage setup · title` | The problem | **—** |  |
| `stage setup · why` | What is being integrated, and over what. The contour integrand is not the posed integrand whenever the substitution has a Jacobian, and conflating the two is the single commonest error in the subject. | The integral to be evaluated, the integrand on the contour, and how the two are related. |  |
| `stage legality · title` | Hypotheses | **—** |  |
| `stage legality · why` | The right to print anything at all: no piece may pass through a singularity, the contour must close, and its orientation must be declared. Run first, because everything downstream is meaningless if it fails. | The contour is closed and meets no singularity; with a branch cut, the integrand is single-valued along it. |  |
| `stage catch · title` | Residues | **—** |  |
| `stage catch · why` | The singular set and the residue sum. The winding number and the enclosed-pole count are separate rows on purpose — one number implying the other is the conflation the dogbone exists to break. | The singularities, their winding numbers $\operatorname{Ind}_\gamma(a)$, and their residues. |  |
| `stage kill · title` | Boundary terms | **—** |  |
| `stage kill · why` | Per-piece disposal. A vanishing piece owes two distinct statements: a bound at the finite limit parameter, and the limit itself. Only the second enters the solve. | Each piece other than the target: a bound at finite $R$ (or $\varepsilon$), and its limit. |  |
| `stage cover · title` | Target | **—** |  |
| `stage cover · why` | The target appears as a labelled piece, under the declared substitution. In the sandbox there is no target, this step is vacuous, and the closed-contour value is the result. | The target integral as a piece of the contour. |  |
| `stage solve · title` | Solution | **—** |  |
| `stage solve · why` | The contour identity, solved for the real integral it was built to find. Worked in units of π throughout, so π is never evaluated and π/2 stays π/2. | The residue theorem, solved for the target. |  |
| `stage verdict · title` | Conclusion | **—** |  |
| `stage verdict · why` | The label is the meet over every step's certificate — computed from what was established, never chosen. One unknown step makes the whole claim unknown; one refusal refuses it. | The rigor of the conclusion is the weakest of its steps: $=$ exact, $\le$ rigorous bound, $\approx$ numerical. |  |

## 5. Derivation statements

Lines the derivation states without a rigor label — an identity, a cross-check, a borrowed value.

| where | today | proposed | flags |
|---|---|---|---|
| `statement 1` <br>*(1 record)* | from boundary terms · the indentation over the real pole z = 0 — contributes −iπ — a known limit, not zero | **—** | maths undelimited |
| `statement 2` <br>*(1 record)* | from boundary terms · the indentation over the real pole z = 0 — contributes −iπ/4 — a known limit, not zero | **—** | maths undelimited |
| `statement 3` <br>*(2 records)* | from boundary terms · the R → ∞ semicircle — contributes −π — a known limit, not zero | **—** | maths undelimited |
| `statement 4` <br>*(2 records)* | from boundary terms · the ρ → 0 indentation over z = 0 — contributes −iπ — a known limit, not zero | **—** | maths undelimited |
| `statement 5` <br>*(92 records)* | independent cross-check — the quadrature agrees with it to 8.88e-16 — agreement is evidence, not proof — the two share no machinery, which is the point | **—** |  |
| `statement 6` <br>*(8 records)* | the residue theorem — ∮ f dz = 2πi [ Σₖ (n(γ,aₖ) − σ)·Res(f,aₖ) − σ·Res(f,∞) ],  σ = n(γ, branch point) | **—** | maths undelimited |
| `statement 7` <br>*(75 records)* | the residue theorem — ∮ f dz = 2πi Σₖ n(γ,aₖ)·Res(f,aₖ) | **—** | maths undelimited |
| `statement 8` <br>*(9 records)* | the residue theorem — ∮ K·f dz = 2πi[ Σ_{\|n\|≤N} Res(K·f, n) + Σⱼ Res(K·f, zⱼ) ],  K = π cot(πz) or π csc(πz) | **—** | maths undelimited |
| `statement 9` <br>*(92 records)* | the system — Σᵢ (aᵢ·t + bᵢ) = ∮ f dz, solved for t — worked in units of π, so π is never evaluated and π/2 stays π/2 | **—** | maths undelimited |

## 6. Bound and limit claims

Composed in `kernel/bounds/*` and `kernel/branch/*`, and displayed as a ledger row's claim. Step
0.3 deliberately left these as strings: each bakes in the numbers next to the arithmetic that
produced them. One row per distinct sentence, numerals masked.

| where | today | proposed | flags |
|---|---|---|---|
| `bound claim 1` <br>*(11 records)* | \|∫ over the arc\| ≤ 5.635e-2 at R = 4, and → 0 as R → ∞, because deg Q − deg P = 4 ≥ 2 makes the bound O(R^-3) | **—** | maths undelimited |
| `bound claim 2` <br>*(4 records)* | \|∫ over the arc\| ≤ 6.629e+0 at ρ = 4.000e+0, and → 0 as R → ∞, because the bound is O(ρ^(-3)·(ln ρ)^2) and a logarithm is weaker than every power | **—** | maths undelimited |
| `bound claim 3` <br>*(10 records)* | \|∫ over the arc\| ≤ 3.175e+0 at ρ = 4.000e+0, and → 0 as R → ∞, because the bound is O(ρ^(-7/10)) and that exponent's sign is negative | **—** | maths undelimited |
| `bound claim 4` <br>*(4 records)* | \|∫ over the arc\| ≤ 2.718e+1 at ρ = 5.000e-2, and → 0 as ε → 0⁺, because the bound is O(ρ^(1)·(ln ρ)^2) and a logarithm is weaker than every power | **—** | maths undelimited |
| `bound claim 5` <br>*(10 records)* | \|∫ over the arc\| ≤ 2.692e+0 at ρ = 5.000e-2, and → 0 as ε → 0⁺, because the bound is O(ρ^(3/10)) and that exponent's sign is positive | **—** | maths undelimited |
| `bound claim 6` <br>*(16 records)* | \|∫ over the cap\| ≤ 5.302e-1 at η = 5.000e-2, and → 0 as η → 0⁺, because the bound is O(η^(1/2)) and that exponent is positive | **—** | maths undelimited |
| `bound claim 7` <br>*(8 records)* | \|∫ over the left side\| ≤ 1.928e+0 at R = 4, and → 0 as R → ∞, because the bound is O(e^{(-3/10)R}) and that exponent is negative | **—** | maths undelimited |
| `bound claim 8` <br>*(1 record)* | \|∫ over the lower semicircle\| ≤ (π/\|a\|)·max\|g\| ≤ 2.094e-1 at R = 4, and → 0 as R → ∞ since max\|g\| = O(R^-2) | **—** | maths undelimited |
| `bound claim 9` <br>*(8 records)* | \|∫ over the right side\| ≤ 3.892e-1 at R = 4, and → 0 as R → ∞, because the bound is O(e^{(-7/10)R}) and that exponent is negative | **—** | maths undelimited |
| `bound claim 10` <br>*(12 records)* | \|∫ over the upper semicircle\| ≤ (π/\|a\|)·max\|g\| ≤ 2.094e-1 at R = 4, and → 0 as R → ∞ since max\|g\| = O(R^-2) | **—** | maths undelimited |
| `bound claim 11` <br>*(4 records)* | \|∫ over the vertical side\| ≤ 1.160e-16 at Re z = 6, and → 0 as R → ∞ because the bound is O(e^{(−1)R²}) and that exponent is negative | **—** | maths undelimited |
| `bound claim 12` <br>*(4 records)* | \|∫ over the vertical side\| ≤ 1.160e-16 at Re z = −6, and → 0 as R → ∞ because the bound is O(e^{(−1)R²}) and that exponent is negative | **—** | maths undelimited |
| `bound claim 13` <br>*(2 records)* | \|∫ over the wedge\| ≤ \|λ\|·1/2·π/(n·c·R^{n−1}) ≤ 1.309e-1 at R = 6, and → 0 as R → ∞ since n = 2 > 1 | **—** | maths undelimited |
| `bound claim 14` <br>*(36 records)* | \|∫ over this side\| ≤ 1.566e+0 at N = 4, and → 0 as N → ∞, because the bound is O((N+½)^-1) | **—** | maths undelimited |
| `bound claim 15` <br>*(3 records)* | the indentation contributes iα·Res = π·(−i), swept angle α = −1π | **—** | maths undelimited |
| `bound claim 16` <br>*(1 record)* | the indentation contributes iα·Res = π·(−i/4), swept angle α = −1π | **—** | maths undelimited |
| `bound claim 17` <br>*(2 records)* | z·f(z) → i, so the arc contributes iα·L = π·(−1) — NOT zero | **—** | maths undelimited |

## 7. Methods

The "how do we know" line under every row.

| where | today | proposed | flags |
|---|---|---|---|
| `method 1` <br>*(8 records)* | decided — not the absence of a pole search | **—** |  |
| `method 2` <br>*(9 records)* | declared by `residueSelection.targetTerms`: the contour's own sides all vanish, so ∮ → 0 and the identity is read backwards as a statement about the sum | **—** | maths undelimited |
| `method 3` <br>*(87 records)* | declared by its role | **—** |  |
| `method 4` <br>*(34 records)* | declared by its role; the coefficient enters Pass 5's M rather than the right-hand side | the piece is a constant multiple of the target; its coefficient enters the linear system |  |
| `method 5` <br>*(83 records)* | declared by the piece list | **—** |  |
| `method 6` <br>*(75 records)* | distance from each pole to each piece | **—** |  |
| `method 7` <br>*(92 records)* | endpoint-to-endpoint check over the piece list | **—** |  |
| `method 8` <br>*(80 records)* | exact arithmetic over ℚ(i) or one quadratic extension of it | **—** | maths undelimited |
| `method 9` <br>*(11 records)* | exact ℚ coefficient bound; no floating point anywhere | **—** | maths undelimited |
| `method 10` <br>*(8 records)* | exact winding numbers against exact exponents, summed over ℚ — the admissibility arithmetic of research 06 §2.1(b), read along the contour | $\sum_j \operatorname{Ind}_\gamma(b_j)\,\alpha_j$, over exact winding numbers and exact exponents |  |
| `method 11` <br>*(92 records)* | exact-sign crossing count over a certified polygonisation | **—** |  |
| `method 12` <br>*(22 records)* | exact-sign segment predicates, and the circle–line quadratic for arcs | **—** |  |
| `method 13` <br>*(4 records)* | imported, not derived here — on Im z = b/2 the integrand collapses to e^{−x²−b²/4}; the remaining ∫ℝe^{−x²}dx = Γ(1/2) = √π is IMPORTED, not derived here | **—** | caps: IMPORTED; maths undelimited |
| `method 14` <br>*(2 records)* | imported, not derived here — on this ray e^{izⁿ} = e^{−tⁿ}; ∫₀^∞e^{−tⁿ}dt = Γ(1+1/n) is IMPORTED (the real substitution u = tⁿ), and the leading minus is the reversed traversal | **—** | caps: IMPORTED; maths undelimited |
| `method 15` <br>*(13 records)* | Jordan's lemma, with max\|g\| from the exact ℚ coefficient bound | **—** | maths undelimited |
| `method 16` <br>*(2 records)* | L6 (oscillatory form), with the range decided in exact ℚ | **—** | lemma number; maths undelimited |
| `method 17` <br>*(4 records)* | L4 at a simple pole; the sign comes from the signed swept angle (θ₁ − θ₀), which is the i·ε prescription in geometric form | **—** | lemma number; maths undelimited |
| `method 18` <br>*(8 records)* | L1, the ML inequality on a vertical side, with max\|f\| the exact maximum of a quadratic in ℚ | **—** | lemma number; maths undelimited |
| `method 19` <br>*(16 records)* | L1, the ML inequality on a vertical side, with the exponent exact in ℚ | **—** | lemma number; maths undelimited |
| `method 20` <br>*(2 records)* | L5: a uniform limit of z·f(z) along an arc of angle 1π | **—** | lemma number; maths undelimited |
| `method 21` <br>*(16 records)* | ML bound for a branch factor on a cap about one of its own branch points | **—** |  |
| `method 22` <br>*(8 records)* | ML bound for R·log^2, exact in ℚ except for ln ρ | **—** | maths undelimited |
| `method 23` <br>*(20 records)* | ML bound for z^α·R, exact in ℚ except for the power ρ^α | **—** | maths undelimited |
| `method 24` <br>*(8 records)* | ML on one side of Γ_N: sup\|K\|·length·max\|f\| = π · 2(N+½) · max\|f\|, exact in ℚ with π and coth(π/2) entering only through certified brackets | **—** | maths undelimited |
| `method 25` <br>*(28 records)* | ML on one side of Γ_N: sup\|K\|·length·max\|f\| = π·coth(π/2) · 2(N+½) · max\|f\|, exact in ℚ with π and coth(π/2) entering only through certified brackets | **—** | maths undelimited |
| `method 26` <br>*(5 records)* | Res(K·f, n) is f(n) over ℚ(i) at every integer (the kernel's own residue is exactly 1 or exactly (−1)ⁿ); Res(K·f, zⱼ) is K(zⱼ)·Res(f,zⱼ), exact because cot and csc are Möbius functions of e^{2πiz₀} | **—** | maths undelimited |
| `method 27` <br>*(4 records)* | Res(K·f, n) is f(n) over ℚ(i) at every integer the kernel alone has a pole at; where the cofactor has one too the orders ADD and the merged residue is the z⁻¹ coefficient of the product's Laurent series, exact in ℚ(i)(π) | **—** | caps: ADD; maths undelimited |
| `method 28` <br>*(22 records)* | research 06 §2.1, decided over ℚ: every genuine branch point is on the forest, every bounded component sums to an integer, and no log is bounded | each bounded cut joins branch points whose exponents sum to an integer; every logarithmic branch point is joined to $\infty$ |  |
| `method 29` <br>*(4 records)* | the hypothesis 'f has no pole at an integer' is SUFFICIENT for the clean form of the theorem and not NECESSARY for the contour argument — the product is meromorphic there, orders ADD, and the merged residue is computed exactly | **—** | caps: SUFFICIENT NECESSARY ADD |
| `method 30` <br>*(3 records)* | the structural route: the roots of a rotated regular n-gon, whose residues sum in the exponent basis with no root ever represented | **—** |  |

## 8. Provenance steps

The ✓/✗ audit trail inside a row's evidence, shown when a row is expanded.

| where | today | proposed | flags |
|---|---|---|---|
| `provenance 1` <br>*(22 records)* | 1 branch point(s), 1 cut(s), no cycle | **—** |  |
| `provenance 2` <br>*(10 records)* | 4J ∈ ℤ, so the factor folds to −1 in ℚ(i) | **—** | maths undelimited |
| `provenance 3` <br>*(2 records)* | 4J ∈ ℤ, so the factor folds to −i in ℚ(i) | **—** | maths undelimited |
| `provenance 4` <br>*(2 records)* | 4J ∈ ℤ, so the factor folds to i in ℚ(i) | **—** | maths undelimited |
| `provenance 5` <br>*(4 records)* | 4J ∉ ℤ, so the factor is carried as an exponential rather than folded | **—** | maths undelimited |
| `provenance 6` <br>*(16 records)* | \|e^{az}\| = e^{Re(a)x − Im(a)y} is bounded on a strip of finite height whatever Im(a) is, so Im(a) cannot change the limit | **—** |  |
| `provenance 7` <br>*(8 records)* | \|log z\| ≤ \|ln ρ\| + 6.283185 on the circle, from the DECLARED determination rather than an assumed 2π | **—** | caps: DECLARED; maths undelimited |
| `provenance 8` <br>*(11 records)* | \|P\| ≤ Σ\|aₖ\|Rᵏ by the triangle inequality, with each \|aₖ\| bounded above in ℚ | **—** | maths undelimited |
| `provenance 9` <br>*(11 records)* | \|Q\| ≥ 2.230e+2 > 0, which also certifies every pole lies strictly inside \|z\| = R | **—** | maths undelimited |
| `provenance 10` <br>*(28 records)* | \|R\| bounded above by exact ℚ coefficient bounds, numerator and denominator separately | **—** | maths undelimited |
| `provenance 11` <br>*(13 records)* | ∫₀^{π} e^{−κ sin ψ} dψ ≤ π/κ for every κ > 0 | **—** | maths undelimited |
| `provenance 12` <br>*(2 records)* | ∫₀^{π/2} e^{−κ sin ψ} dψ ≤ π/(2κ) for every κ > 0 | **—** | maths undelimited |
| `provenance 13` <br>*(3 records)* | a fifth or seventh root of −1 needs a degree-4 or degree-6 field — which is why the per-pole route declined, and why this one is not the same claim | **—** |  |
| `provenance 14` <br>*(6 records)* | an independent check: the quadrature of this piece is 3.07e-13 away (2.2e-11% of it) — at a finite limit parameter that gap is the piece's own tail | **—** |  |
| `provenance 15` <br>*(8 records)* | bounded component {b1, b2}: Σα = -1/1 ∈ ℤ | **—** | maths undelimited |
| `provenance 16` <br>*(4 records)* | crossing 'Γ' ADDS 2πi to the logarithm: its monodromy has infinite order, so no factor multiplies the integrand and no sheet count closes the loop | **—** | caps: ADDS; maths undelimited |
| `provenance 17` <br>*(3 records)* | crossing 'Γ' multiplies the integrand by e^(2πi·1/2) = −1 | **—** | maths undelimited |
| `provenance 18` <br>*(1 record)* | crossing 'Γ' multiplies the integrand by e^(2πi·13/10), which is e^(2πi·3/10) with the integer part of J dropped | **—** | maths undelimited |
| `provenance 19` <br>*(2 records)* | crossing 'Γ' multiplies the integrand by e^(2πi·−1/2) = −1, which is e^(2πi·1/2) with the integer part of J dropped | **—** | maths undelimited |
| `provenance 20` <br>*(1 record)* | crossing 'Γ' multiplies the integrand by e^(2πi·−1/4) = −i, which is e^(2πi·3/4) with the integer part of J dropped | **—** | maths undelimited |
| `provenance 21` <br>*(3 records)* | crossing 'Γ' multiplies the integrand by e^(2πi·−7/10), which is e^(2πi·3/10) with the integer part of J dropped | **—** | maths undelimited |
| `provenance 22` <br>*(1 record)* | crossing 'Γ1' multiplies the integrand by e^(2πi·1/2) = −1 | **—** | maths undelimited |
| `provenance 23` <br>*(1 record)* | crossing 'Γ1' multiplies the integrand by e^(2πi·3/4) = −i | **—** | maths undelimited |
| `provenance 24` <br>*(2 records)* | crossing 'Γ1' multiplies the integrand by e^(2πi·1/4) = i | **—** | maths undelimited |
| `provenance 25` <br>*(4 records)* | crossing 'Γ1' multiplies the integrand by e^(2πi·−1/2) = −1, which is e^(2πi·1/2) with the integer part of J dropped | **—** | maths undelimited |
| `provenance 26` <br>*(13 records)* | deg Q − deg P = 2 ≥ 1, so max\|g\| → 0 | **—** | maths undelimited |
| `provenance 27` <br>*(1 record)* | e^(2πi·13/10) = e^(2πi·3/10), because e^(−2πi) = 1 — the same number, and the literal form is the one the integrand's exponent gives | **—** | maths undelimited |
| `provenance 28` <br>*(7 records)* | e^(2πi·1/2) is already reduced: J ∈ [0, 1) | **—** | maths undelimited |
| `provenance 29` <br>*(10 records)* | e^(2πi·−7/10) = e^(2πi·3/10), because e^(−2πi) = 1 — the same number, and the literal form is the one the integrand's exponent gives | **—** | maths undelimited |
| `provenance 30` <br>*(8 records)* | e^{max Re Q} is transcendental, so the bound's VALUE is a float — the limit is what the lemma needs, and that rests on the sign alone | **—** | caps: VALUE |
| `provenance 31` <br>*(16 records)* | e^{κR} is transcendental, so the bound's VALUE is a float — the limit is what the lemma needs, and that rests on the sign alone | **—** | caps: VALUE |
| `provenance 32` <br>*(2 records)* | established by: Jordan's inequality sin ψ ≥ 2ψ/π on [0, π/2] | **—** | maths undelimited |
| `provenance 33` <br>*(13 records)* | established by: Jordan's inequality sin ψ ≥ 2ψ/π on [0, π/2], extended to [0, π] by sin ψ = sin(π − ψ) | **—** | maths undelimited |
| `provenance 34` <br>*(8 records)* | J = 1/2, exactly — Σα over the arc's "from" side | **—** | maths undelimited |
| `provenance 35` <br>*(10 records)* | J = −7/10, exactly — Σα over the arc's "from" side | **—** | maths undelimited |
| `provenance 36` <br>*(8 records)* | ln ρ is a float, so the bound's VALUE is a float — the limit is what the lemma needs, and that rests on the sign alone | **—** | caps: VALUE; maths undelimited |
| `provenance 37` <br>*(36 records)* | max\|f\| is read at \|z\| = N+½ and bounds \|f\| on the whole square: U(r)/L(r) decreases in r once both are divided by r^{deg D}, so the closest point of Γ_N is the worst | **—** |  |
| `provenance 38` <br>*(4 records)* | max\|f\| on the side is ATTAINED, not majorised: Re Q(c+iy) is the exact quadratic 1y² + −17/10y + −36, whose maximum on the segment is at y = 0 | **—** | caps: ATTAINED; maths undelimited |
| `provenance 39` <br>*(4 records)* | max\|f\| on the side is ATTAINED, not majorised: Re Q(c+iy) is the exact quadratic 1y² + −1y + −36, whose maximum on the segment is at y = 0 | **—** | caps: ATTAINED; maths undelimited |
| `provenance 40` <br>*(4 records)* | n(γ, z = -1) = −1, n(γ, z = 1) = −1 — non-zero, and the SUM is what has to be an integer | **—** | caps: SUM; maths undelimited |
| `provenance 41` <br>*(4 records)* | n(γ, z = 0) = −1, n(γ, z = b) = −1 — non-zero, and the SUM is what has to be an integer | **—** | caps: SUM; maths undelimited |
| `provenance 42` <br>*(4 records)* | refusing would be wrong (the answer is correct) and warning would be wrong (nothing is uncertain); what the escalation costs the record is a DECLARED merged order and residue, both checked against the engine's own | **—** | caps: DECLARED; jargon: the record |
| `provenance 43` <br>*(36 records)* | research 03 §8 states this bound without the π from π cot(πz), and it is then not an upper bound at all — 3.392 against a measured 3.567 at N = 3 (finding D-2) | *(delete)* |  |
| `provenance 44` <br>*(28 records)* | sup\|cot πz\| = coth(π(N+½)) ≤ coth(π/2), attained at N = 0 — one constant for the whole family, which is what the limit needs | **—** | maths undelimited |
| `provenance 45` <br>*(8 records)* | sup\|csc πz\| = 1 on Γ_N for EVERY N: ≤ 1/sinh(π/2) on the horizontal sides and ≤ 1 on the vertical ones | **—** | caps: EVERY; maths undelimited |
| `provenance 46` <br>*(13 records)* | the bound is independent of R, beating plain ML by the factor \|a\|R | **—** |  |
| `provenance 47` <br>*(2 records)* | the bound is O(R^-1), and 1 − n < 0 exactly when n > 1 | **—** |  |
| `provenance 48` <br>*(16 records)* | the cofactor is shifted to the cap's own centre by exact synthetic division over ℚ(i), so the bound is the one this file already states, read about w = z − b | **—** | maths undelimited |
| `provenance 49` <br>*(4 records)* | the exponent -3 = 1 + (deg P − deg Q) is an exact integer, so its SIGN is decided; the log cannot change it | **—** | caps: SIGN |
| `provenance 50` <br>*(4 records)* | the exponent 1 = 1 + (ord₀P − ord₀Q) is an exact integer, so its SIGN is decided; the log cannot change it | **—** | caps: SIGN |
| `provenance 51` <br>*(8 records)* | the exponent -3/10 = −Re(a) − ord₀N + ord₀D is an exact rational, so its SIGN is decided | **—** | caps: SIGN |
| `provenance 52` <br>*(8 records)* | the exponent -7/10 = Re(a) + deg N − deg D is an exact rational, so its SIGN is decided | **—** | caps: SIGN |
| `provenance 53` <br>*(10 records)* | the exponent -7/10 = α + 1 + (deg P − deg Q) is an exact rational, so its SIGN is decided | **—** | caps: SIGN; maths undelimited |
| `provenance 54` <br>*(10 records)* | the exponent 3/10 = α + 1 + (ord₀P − ord₀Q) is an exact rational, so its SIGN is decided | **—** | caps: SIGN; maths undelimited |
| `provenance 55` <br>*(16 records)* | the exponent 1/2 = α_b + 1 + (ord₀Ñ − ord₀D̃) is an exact rational, so its SIGN is decided; α_b > −1 is the integrability of the endpoint singularity, spent here | **—** | caps: SIGN; maths undelimited |
| `provenance 56` <br>*(8 records)* | the limit rests on Re(q₂) = −1 alone: the rectangle's height is fixed, so as R → ∞ the vertex leaves the segment and the bound is O(e^{Re(q₂)R²}) | **—** | maths undelimited |
| `provenance 57` <br>*(8 records)* | this is the dogbone's licence: it winds about both ends of a bounded cut, and the two turns cancel in the exponent | **—** |  |
| `provenance 58` <br>*(5 records)* | Γ(1/2) = √π — the Gaussian ∫ℝ e^{−x²}dx = √π, established by polar coordinates, not by any contour | **—** | maths undelimited |
| `provenance 59` <br>*(1 record)* | Γ(4/3) = ∫₀^∞ t^{1/3}e^{−t}dt — the Gamma function at a rational argument, established by the real substitution u = tⁿ and not by any contour | **—** | maths undelimited |
| `provenance 60` <br>*(16 records)* | η^{α} and the other factors' powers are irrational powers of rationals, so the bound's VALUE is a float — the limit is what the lemma needs, and that rests on the sign alone | **—** | caps: VALUE; maths undelimited |
| `provenance 61` <br>*(16 records)* | η² < \|b − bⱼ\|² at every other branch point, checked exactly over ℚ, so the 1 remaining factor is bounded above and below and move the VALUE without moving the exponent | **—** | caps: VALUE; maths undelimited |
| `provenance 62` <br>*(11 records)* | π bounded above by a certified rational (Machin + alternating series) | **—** | maths undelimited |
| `provenance 63` <br>*(20 records)* | ρ^α is an irrational power of a rational, so the bound's VALUE is a float — the limit is what the lemma needs, and that rests on the sign alone | **—** | caps: VALUE; maths undelimited |
| `provenance 64` <br>*(2 records)* | ψ = nθ carries the arc's range 1π/4 to 1π/2, which is what the predicate was asked about | **—** | maths undelimited |

## 9. Restrictions

| where | today | proposed | flags |
|---|---|---|---|
| `restriction 1` <br>*(36 records)* | N + ½ = 9/2; deg D − deg N = 2 | **—** |  |
