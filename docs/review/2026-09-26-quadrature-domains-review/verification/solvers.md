# Verification — solvers slice (adversarial pass)

Independent probes v1–v13.mjs (`scratchpad/verify-solvers/`), booting via `app/workers/solver-graph.mjs`. No tracked file edited.

| ID     | Verdict                    | Final severity          | Reachability / user outcome                                                                                                                                                                                                                                                                             |
| ------ | -------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SOLV-1 | CONFIRMED                  | P1                      | Bounded PQD, non-integer α (incl. ½ quick-pick), auto w₀ = pole centroid, poles straddling ℝ₋ → "No algebraic root found" for a genuine domain. No wrong domain drawn.                                                                                                                                  |
| SOLV-2 | CONFIRMED                  | P1                      | Manual w₀ across ℝ₋ from a single pole → definitive but false "no bounded PQD with φ(0)=w₀ exists" (closed-form domain passes the identity at 2.6e-16; app bound 1.78 vs consistent-sheet 0.0049).                                                                                                      |
| SOLV-3 | CONFIRMED                  | P1                      | Custom h text + share-link restore. `1/((w-1)^3*(w+1))` → 4 simple poles, residues ~1e11, no warning; grid rebuilt at 6 sf gives h off by 1e11; solve fails. A combined rational with a repeated root ≠ 0 whose true QD exists also fails; sum-of-principal-parts form and w^k factors parse correctly. |
| SOLV-4 | PLAUSIBLE (code confirmed) | P2 latent               | No empty-test-set guard in `solver-uqd-pqd-singular.mjs:421-447`; ~20 preset solves always found 3 test points.                                                                                                                                                                                         |
| SOLV-5 | CONFIRMED                  | P2 (see coupling below) | Unbounded h = 1.5/w + 0.5/w², c ≈ 1.43–1.45 → "⚠ Quadrature identity not satisfied" for a genuine univalent QD (6000 samples 1.2e-3 vs 16000 1.1e-10). Second mechanism: the doubling loop exits early on "improved < 0.7×" at c = 1.45.                                                                |
| SOLV-6 | CONFIRMED                  | P2                      | (a) Continuation to c ≈ 1.48 lands on a branch with a φ′ zero at \|z\| = 1.00003 (non-univalent); `isBoundaryUnivalent` true at 500/5000 samples. (b) z + a z², a = 0.5001–0.501: live-drag frames (96 samples) show ✓ for a non-univalent map; drag-end solve recovers the correct root.               |

**Branch convention (SOLV-1/2).** Thesis Def/Thm 4.2.1 states the PQD identity through a single-valued expression; the thesis never makes a_j^α principal. The anchored (continuous) sheet is correct. **Same defect class as SCH-2 but a different site, trigger and fix** — fixing one leaves the other broken. Consistent fix: locator φ_anchored(z_j) = a_j (branch-free, as LQD does) and p^{1−α} := a_j / R#(z_j); rotating w₀ onto ℝ₊ is only partial.

## New items

1. **[P1, latent P0] SOLV-5 and SOLV-6 are coupled.** On the non-univalent continuation branch at c = 1.478 the identity at 64k samples is 3.3e-8 (< 1e-6 tol) and `isBoundaryUnivalent(500)` is true — only the 6000-sample cap currently prevents "✓ Valid" on a non-univalent map. A local-univalence (argument-principle φ′-zero count) check must land with or before any escalation fix.
2. **[P2]** UQD escalation exits early (see SOLV-5), so "raise the cap" alone is insufficient.
3. **[P3, API trap]** `solveInverseQD` with `warmPhi` keeps the seed's c instead of `opts.c`; UI and cmax patch the seed, so not user-visible.
