{
  "name": "@cas/schwarz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Schwarz-reflection σ engine: reconstructs σ(w)=conj(F(φ⁻¹(w))) from a closed-form φ (numerical exterior-branch inverse) for classical unbounded-Laurent quadrature domains. Shared by Correspondences and Complex Dynamics (ADR-0007). Convention-neutral, on @cas/core. Consumed via SOURCE by the suite's Vite/Vitest bundlers (no dist build).",
  "license": "MIT",
  "author": "Andrew Graven <andrew@graven.com>",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@cas/core": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src test"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=22"
  }
}
